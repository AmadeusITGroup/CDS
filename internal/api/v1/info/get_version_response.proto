syntax = "proto3";

package api.v1.info;
option go_package = "github.com/amadeusitgroup/cds/cdspb";

// GetVersionResponse carries the current agent version string.
message GetVersionResponse {
  // current is the version string reported by the running agent.
  string current = 1;
}